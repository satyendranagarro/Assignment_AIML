# Locked stack A — Phase 0 baseline (pin loosely; tighten in Phase 6 if needed)
python-dotenv>=1.0.1
pyyaml>=6.0.2
httpx>=0.27.0
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
tenacity>=9.0.0

# Orchestration + LLM
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-chroma>=0.1.4
langchain-google-genai>=2.0.0
openai>=1.50.0

# Stores
chromadb>=0.5.0
neo4j>=5.25.0

# UI
streamlit>=1.38.0

# MCP
mcp>=1.0.0,<2

# Cursor SDK (online LLM_PROVIDER=cursor without local gateway)
cursor-sdk>=1.0.0

# Tests
pytest>=8.3.0
pytest-asyncio>=0.24.0
